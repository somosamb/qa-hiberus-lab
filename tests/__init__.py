# ---------------------------------------------------------------------------
# tests/__init__.py
#
# This file marks the `tests` directory as a Python package.
# Required for clean relative imports and to avoid module name collisions
# when pytest collects tests across multiple directories.
# ---------------------------------------------------------------------------
