# ---------------------------------------------------------------------------
# pages/__init__.py
#
# This file marks the `pages` directory as a Python package.
# Without it, Python cannot resolve imports like:
#   from pages.login_page import LoginPage
#
# The file is intentionally empty — its presence is all that is needed.
# ---------------------------------------------------------------------------
